import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '80%',
    marginBottom: 10,
  },
  input: {
    borderBottomWidth: 1,
    borderColor: 'gray',
    paddingLeft: 10,
    height: 40,
    fontSize: 20,
  },
});
